{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "openpersona/acn/acn-register",
  "title": "ACN Register Config",
  "description": "ACN AgentRegisterRequest configuration generated by OpenPersona. owner and endpoint are runtime-filled placeholders.",
  "type": "object",
  "required": ["name", "skills"],
  "properties": {
    "owner": {
      "type": "string",
      "description": "Agent owner identifier — placeholder at generation time, filled by host at runtime (e.g. 'user-{id}')"
    },
    "name": {
      "type": "string",
      "description": "Agent name (from personaName)"
    },
    "endpoint": {
      "type": "string",
      "description": "Agent A2A endpoint URL — placeholder at generation time, filled by host at runtime"
    },
    "skills": {
      "type": "array",
      "items": { "type": "string" },
      "description": "Agent skill IDs (extracted from agent-card.json skills)"
    },
    "agent_card": {
      "type": "string",
      "description": "Relative path to the Agent Card file",
      "default": "./agent-card.json"
    },
    "subnet_ids": {
      "type": "array",
      "items": { "type": "string" },
      "description": "Subnets to join on registration",
      "default": ["public"]
    }
  },
  "additionalProperties": false
}
